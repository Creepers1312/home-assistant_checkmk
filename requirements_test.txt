# pulls in: pytest, pytest-asyncio, homeassistant (and therefore aiohttp).
# Pinning the upper bound lets dependabot bump us in lockstep with HA releases.
pytest-homeassistant-custom-component>=0.13.166
